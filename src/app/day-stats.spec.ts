/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {DayStats} from './day-stats';

describe('DayStats', () => {
  it('should create an instance', () => {
    expect(new DayStats()).toBeTruthy();
  });
});
