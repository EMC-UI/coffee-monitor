/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {HourStats} from './hour-stats';

describe('HourStats', () => {
  it('should create an instance', () => {
    expect(new HourStats()).toBeTruthy();
  });
});
